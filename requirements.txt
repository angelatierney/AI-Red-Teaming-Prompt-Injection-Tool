# AI Red Teaming & Prompt Injection Tool - Dependencies
# Compatible with ARM64/Raspberry Pi OS (64-bit)
# Python 3.8+

# Core HTTP library for API requests
requests>=2.31.0

# Data analysis and logging
pandas>=2.0.0

# Optional: For advanced retry logic (already included via requests)
urllib3>=2.0.0

# Optional: For future integration with garak (NVIDIA's vulnerability scanner)
# garak>=0.9.0  # Uncomment when ready to integrate

# Optional: For future integration with PyRIT (Microsoft's red teaming toolkit)
# pyrit>=1.0.0  # Uncomment when ready to integrate
