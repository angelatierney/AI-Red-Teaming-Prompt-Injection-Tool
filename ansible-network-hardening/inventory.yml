---
# Example inventory - replace with your load balancer host(s)
all:
  children:
    loadbalancers:
      hosts:
        lb01:
          ansible_host: 192.168.1.10
        # lb02:
        #   ansible_host: 192.168.1.11
      vars:
        ansible_user: ubuntu
        # ansible_ssh_private_key_file: ~/.ssh/your_key
